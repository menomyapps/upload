# B"H

# ls
alias ll='ls -la'
alias lll='ls -la'
alias l='ls -la'

# docker 
alias docExec='docker exec -it'
alias docRun='docker run -dit --rm --name'
alias docPS='docker ps'


# Git
alias commit='git commit -m '

alias netstatDefault='netstat -atup -c'
